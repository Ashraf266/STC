import { WordsCropPipe } from './words-crop.pipe';

describe('WordsCropPipe', () => {
  it('create an instance', () => {
    const pipe = new WordsCropPipe();
    expect(pipe).toBeTruthy();
  });
});
